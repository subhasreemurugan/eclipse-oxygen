package hashTable;

public class uniqchars {
	public static void main(String Args[]) {
		String s= "gfdsh mnb";
		int count =0;
		for (int i =1;i<s.length();i++) {
			if(s.indexOf(count)== s.indexOf(i)) {
				System.out.println("not all unique");
				break;
			}
			count++;
		}
		System.out.println(count);
	}
}
